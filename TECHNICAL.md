# 方法与实现

本文说明数据口径、因子定义、预测模型、组合优化、执行假设和复现方式。公开结果均为 Tushare Pro 数据上的聚合统计，不包含可还原的行情、信号、持仓或成交明细。

## 1. 研究范围

- 股票池：历史中证 500 成分股；基准代码 `000905.SH`。
- 决策频率：每 5 个开放交易日一次；信号在收盘后生成，订单在下一开放日开盘执行。
- 标签：个股从 $t+1$ 开盘至 $t+6$ 开盘的收益相对同期中证 500 全收益指数作几何超额，$r^A=(1+r^P)/(1+r^B)-1$。
- 风险窗口：252 个交易日；因子最长回看窗口为 250 个交易日。
- 基准规模：1 亿元；单票 ADV 参与率上限为 5%。

| 区间 | 用途 |
|---|---|
| 2016 | 因子与风险窗口预热 |
| 2017—2019 | 首个年度折的初始训练 |
| 2020—2025 | 六个不重叠的年度滚动外推折 |
| 2026-01—2026-06 | 已揭盲失效样本；仅作冻结后回归检查 |

研究时钟如下：

```text
t 日及以前可得数据
  → t 日收盘后计算因子
  → 仅使用 label_available_date < t 的样本拟合
  → 生成截面得分和预期主动收益
  → 求解目标权重
  → t+1 日开盘尝试成交
  → 未成交部分继续保留
```

标签在 $t+6$ 开盘后才视为可得。训练样本同时执行 5 个开放交易日的隔离，防止标签窗口与评价窗口重叠。2026 年窗口已用于诊断 v1 失效，不再作为独立样本外证据。

## 2. 数据合同

### 2.1 输入

数据接口包括交易日历、指数权重、股票与指数日线、复权因子、涨跌停价格、每日指标、证券主表、停复牌、历史名称、申万行业，以及财务指标、利润表、资产负债表、现金流量表和财报披露日期。

| 数据 | 时点处理 |
|---|---|
| 指数成分与权重 | 成分按中证指数公告的实际生效日切换；权重使用严格早于决策日的最近月末截面，尚未进入该截面的调入股使用冻结代理权重并保留来源标记 |
| 行业 | 使用有效起止区间；2021-12-13 前后分别采用申万 2014 与 2021 分类 |
| 名称与 ST | 按历史有效区间判断，不以当前名称回填历史 |
| 停复牌与涨跌停 | 按执行日状态限制买卖方向 |
| 价格与复权 | 因子使用当日收盘后可得的复权历史，交易使用下一日复权开盘价 |
| 每日指标 | 当日收盘后可得，最早用于下一开放日交易 |
| 财务报表 | 按公告日期窗口请求，再独立限制报告期；取公告日、实际公告日与披露日期中的最晚值，下一开放日才可用 |

原始响应按请求参数缓存；行情标准表由年度分区物化，财务标准表按接口汇总。两类数据都对字段、主键、日期、覆盖率和跨表关系执行质量检查。研究运行记录实际加载表的 SHA-256；配置、源码或数据内容变化会形成不同的运行身份。

指数成分标准层由两张表组成：`benchmark_membership_events` 保存公告日、生效日、调入调出和公告来源，`benchmark_membership_intervals` 保存每只股票不重叠的有效区间。Tushare 月末权重用于识别并对账成分差分，不用于推迟已经生效的调整。若调样已生效而确认该变更的月末权重尚不可得，调入股暂时承接同一事件调出股的合计权重并平均分配；下一份已知月末截面可用后自动恢复官方截面权重。质量门禁检查事件增删对称、公告先于生效、有效日为开放日、区间不重叠、每日 500 只成员及月末快照集合一致性。

财务接口按历史成分股逐只请求。Tushare 财务接口的 `start_date` 与 `end_date` 表示公告日期，下载器按该窗口请求并在响应中独立筛选 `report_period`；单次返回触及接口行数上限时，继续拆分公告日期窗口。标准层保留同一股票、同一报告期的全部不同修订版，并记录 `source_announcement_date`、`available_date`、`version_sequence` 和供应商行哈希。研究时点查询只选择 `available_date` 不晚于决策日的最新版本；缺失公告日不得以报告期代替。财务数据合同同时要求最新目标报告期的股票覆盖率不低于 90%，以阻断公告窗口与报告期混用造成的末期缺数。

公开仓库不包含 `.env`、供应商逐行数据、请求数据库、信号、持仓、成交和完整指数权重。聚合报告在生成时校验来源指纹，并只输出 PNG 和不可逆聚合 JSON。

### 2.2 截面股票池

决策日股票必须同时满足：属于当时有效的中证 500 成分、具备足够价格历史、名称与 ST 状态允许建仓、必要行业与风险输入可用。缺失行业进入独立未知组；已有持仓若失去建仓资格仍可减仓，不允许新增主动仓位。

## 3. 因子目录

记 $P_t$ 为复权收盘价，$p_t=\log P_t$，$r_t=p_t-p_{t-1}$，$r_t^m$ 为中证 500 对数收益，$TO_t$ 和 $TO_t^f$ 分别为总换手率和自由流通换手率。$\overline{x}_{t,n}$ 表示最近 $n$ 期均值。方向 $s$ 用于把因子统一为“数值越高，预期收益越高”；合成前使用 $s\times f$。

| 家族 | 因子 | 原始公式 | $s$ |
|---|---|---|---:|
| 反转 | `reversal_1d` | $-(p_t-p_{t-1})$ | 1 |
| 反转 | `reversal_5d` | $-(p_t-p_{t-5})$ | 1 |
| 反转 | `reversal_10d` | $-(p_t-p_{t-10})$ | 1 |
| 反转 | `reversal_20d` | $-(p_t-p_{t-20})$ | 1 |
| 动量 | `momentum_20_5` | $p_{t-5}-p_{t-20}$ | 1 |
| 动量 | `momentum_60_20` | $p_{t-20}-p_{t-60}$ | 1 |
| 动量 | `momentum_120_20` | $p_{t-20}-p_{t-120}$ | 1 |
| 动量 | `momentum_250_20` | $p_{t-20}-p_{t-250}$ | 1 |
| 风险 | `low_vol_20` | $-\operatorname{Std}_{20}(r)$ | 1 |
| 风险 | `low_downside_vol_60` | $-\sqrt{\overline{\min(r,0)^2}_{t,60}}$ | 1 |
| 风险 | `low_range_vol_20` | $-\sqrt{\overline{\log^2(H/L)}_{t,20}/(4\log 2)}$ | 1 |
| 风险 | `beta_60` | $\operatorname{Cov}_{60}(r,r^m)/\operatorname{Var}_{60}(r^m)$ | -1 |
| 风险 | `low_idio_vol_60` | $-\sqrt{\operatorname{Var}_{60}(r)-\operatorname{Cov}_{60}(r,r^m)^2/\operatorname{Var}_{60}(r^m)}$ | 1 |
| 风险 | `skewness_60` | $\operatorname{Skew}_{60}(r)$ | -1 |
| 风险 | `max_return_20` | $\max_{20}(r)$ | -1 |
| 流动性 | `amihud_20` | $\log\overline{(|r|/\mathrm{Amount})}_{t,20}$ | 1 |
| 流动性 | `free_turnover_20` | $\log\overline{TO^f}_{t,20}$ | -1 |
| 流动性 | `turnover_shock_5_60` | $\log(\overline{TO}_{t,5}/\overline{TO}_{t,60})$ | -1 |
| 流动性 | `zero_return_20` | $\overline{\mathbf 1(|r|\le 10^{-12})}_{t,20}$ | 1 |
| 流动性 | `close_location_20` | $\overline{(2C-H-L)/(H-L)}_{t,20}$ | 1 |
| 规模 | `size` | $-\log(\mathrm{流通市值})$ | 1 |
| 规模 | `total_size` | $-\log(\mathrm{总市值})$ | 1 |
| 规模 | `free_float_ratio` | $\log(\mathrm{流通市值}/\mathrm{总市值})$ | 1 |
| 估值 | `book_to_price` | $\log(1/PB)$ | 1 |
| 交互 | `abnormal_turnover_reversal` | $\mathrm{reversal}_{5d}\max(\mathrm{turnover\_shock}_{5,60},0)$ | 1 |

价量因子与下列基本面候选现已进入同一时点特征接口。基本面值只从公告后的下一开放日生效；覆盖不足、数据陈旧或公告时点违规会在进入筛选器前关闭。

### 3.1 基本面候选因子

利润表和现金流量表的季报值为年初至报告期累计值。计算器先还原单季流量，再对连续四季求和；资产和权益使用当期与上年同期平均值。每个决策日只读取当时已经可用的最新修订版，报告期距决策日超过 180 天时整组关闭。银行、保险和证券公司的收入成本、经营现金流、资本开支、资产增长及应计因子暂不跨行业比较，只保留 ROE、ROA、账面市值比、盈利收益率及盈利增长等口径较一致的指标。

| 家族 | 因子 | 原始公式 | 方向 |
|---|---|---|---:|
| 质量 | `gross_profitability_ttm` | $(Revenue_{TTM}-OperCost_{TTM})/AvgAssets$ | + |
| 质量 | `roe_ttm` | $NetIncome_{TTM}/AvgEquity$ | + |
| 质量 | `roa_ttm` | $NetIncome_{TTM}/AvgAssets$ | + |
| 质量 | `operating_margin_ttm` | $OperatingProfit_{TTM}/Revenue_{TTM}$ | + |
| 质量 | `cash_return_on_assets_ttm` | $CFO_{TTM}/AvgAssets$ | + |
| 价值 | `book_to_market` | $Equity/MV$ | + |
| 价值 | `earnings_yield_ttm` | $NetIncome_{TTM}/MV$ | + |
| 价值 | `sales_to_price_ttm` | $Revenue_{TTM}/MV$ | + |
| 价值 | `cfo_yield_ttm` | $CFO_{TTM}/MV$ | + |
| 成长 | `revenue_growth_ttm_yoy` | $Revenue_{TTM}/Revenue_{TTM-1y}-1$ | + |
| 成长 | `earnings_growth_ttm_yoy` | $(NI_{TTM}-NI_{TTM-1y})/\lvert NI_{TTM-1y}\rvert$ | + |
| 成长 | `cfo_growth_ttm_yoy` | $(CFO_{TTM}-CFO_{TTM-1y})/\lvert CFO_{TTM-1y}\rvert$ | + |
| 成长 | `roe_change_yoy` | $ROE_{TTM}-ROE_{TTM-1y}$ | + |
| 投资 | `asset_growth_yoy` | $Assets_t/Assets_{t-1y}-1$ | − |
| 投资 | `capex_intensity_ttm` | $Capex_{TTM}/AvgAssets$ | − |
| 应计 | `total_accruals_ttm` | $(NetIncome_{TTM}-CFO_{TTM})/AvgAssets$ | − |
| 应计 | `cash_conversion_ttm` | $CFO_{TTM}/\lvert NetIncome_{TTM}\rvert$ | + |

上述 17 个基本面候选与 25 个价量候选组成冻结的 A0 42 因子池。这里的方向是待检验的经济先验；是否启用仍由逐年外推中的覆盖率、尾部收益、方向稳定性和相关性门禁决定。

### 3.2 A2 价量与涨跌停候选

A2 日频目录与 A0 物理分离。`builtin_daily` 和 `builtin_daily_fundamental` 仍只暴露原目录；只有显式选择 `builtin_a2_daily` 或 `builtin_a2_daily_fundamental` 才能使用下列候选。这样旧配置中的空因子列表不会因代码升级而改变研究池。

记 $\hat\beta^{60}_{i,t-1}$ 为截至前一开放日估计的 60 日市场 Beta，$U_t$ 为当日涨停价。三项交易拥挤因子在发现期显示反向关系，v2 的方向按“数值越高，预期收益越高”统一如下：

| 家族 | 因子 | 有向公式 |
|---|---|---|
| 反转 | `market_residual_reversal_20` | $-\sum_{j=0}^{19}(r_{i,t-j}-\hat\beta^{60}_{i,t-j-1}r^m_{t-j})$ |
| 动量 | `market_residual_momentum_120_20` | $\sum_{j=20}^{119}(r_{i,t-j}-\hat\beta^{60}_{i,t-j-1}r^m_{t-j})$ |
| 流动性 | `turnover_volatility_20` | $-\operatorname{Std}_{20}[\log(1+TO)]$ |
| 交易 | `high_turnover_return_20` | $-\sum_{20}r_t\max[\log(TO_t/\overline{TO}_{60}),0]$ |
| 交易 | `intraday_strength_20` | $-\overline{\log(C/O)}_{20}$ |
| 涨跌停 | `limit_up_close_rate_20` | $-\overline{\mathbf1(C\ge U)}_{20}$ |
| 涨跌停 | `failed_limit_up_rate_20` | $-\overline{\mathbf1(H\ge U,C<U)}_{20}$ |

代码保存的是表中去掉最外层方向后的原始量，并在筛选与合成时应用目录方向。残差收益使用滞后 Beta，涨跌停事件使用未复权价格与同日法定价格区间；全部信息在 $t$ 日收盘后形成，最早用于 $t+1$ 日开盘交易。

## 4. 截面处理与因子筛选

每个决策日、每个因子独立执行以下处理：

1. 检查截面覆盖率；
2. 以中位数绝对偏差进行 5 倍稳健缩尾；
3. 对数流通市值和当时有效行业哑变量回归中性化；
4. 对回归残差作截面标准化。

因子筛选只读取拟合日以前已经成熟的标签，最多使用最近 156 个训练截面。每个拟合点重新计算：总体覆盖率、有效截面比例、有向秩相关、方向一致率、Newey–West 统计量、Benjamini–Hochberg 修正后的 q 值、五分组单调性、相邻截面排名变化率，以及计入双边线性费用和卖出印花税后的 Q5−Q1 收益。训练窗口再切成四个连续时间块，同时检查块内有向秩相关和费后价差。

候选因子按上述证据排序。绝对秩相关高于 0.75 的因子通过传递闭包归为同一相关簇，每簇至多保留 1 个；每个经济因子族至多保留 2 个，总数上限为 10。最低 5 个不是补足目标，而是有效性门槛：若硬门槛、因子族限额和相关簇限额之后少于 5 个，筛选结果直接为空。系统不会再以 `selected_by_min_factor_fallback` 把失败因子送入模型。

研究消融可在相同特征合同下登记 `excluded_factors`。被排除者在统计计算前移出候选集合，并随拟合记录保存；该机制用于比较冻结 A0 与 A2 扩展池，不能在看到某一年度组合收益后临时修改。

每次拟合保存全部统计量、q 值、相关簇、门槛、初选名单、最终名单和拒绝原因。若一次重训练返回空名单，信号引擎立即停用旧模型；组合层把全截面解释为零预测收益，在交易与风险约束允许时回到基准，而不是沿用陈旧信号或停留在现金。

滚动筛选对已经成熟的“日期 × 因子”局部统计采用增量缓存；拟合日严格递增时只计算新进入窗口的日期，非顺序调用、候选集合改变或同一日期新增成熟标签时自动失效相应缓存。缓存最多保留当前 156 个截面，不改变全窗口 Spearman 相关、Newey–West、分段检验或重拟合频率。2020 年真实信号阶段的 195 个截面和 120 次拟合耗时 508.7 秒；97,500 行信号与旧制品在 $10^{-13}$ 精度内一致，首次非逐位一致的审计浮点字段仅相差 $3.5\times10^{-18}$。

## 5. 因子族合成与收益校准

通过筛选的因子先按固定经济类别形成族内方向等权得分，再在因子族之间分配权重。所有方法共用单因子 20%、单因子族 35%、至少 5 个有效因子和至少 3 个有效因子族的约束；约束容量不足时拟合失败并关闭主动信号。当前方法筛选配置比较三种合成器：

1. `family_direction_equal`：因子族等权；若某族受单因子容量限制，剩余权重按比例分配给其他因子族；
2. `family_ridge`：对因子族得分进行系数非负的滚动 Ridge，再投影到相同权重上限；
3. `family_robust_ic`：以训练窗口内因子族秩相关中位数乘正向截面比例作为权重依据，非正因子族权重为零。

个股缺失部分因子时，先在族内按可用因子重新归一化，再在可用因子族之间重新归一化；低于配置的因子或因子族可用比例时不输出得分。模型拟合记录因子族构成、原始依据、族权重、因子权重、权重上限命中和有效因子数。

### 5.1 历史基线：IC 的经验贝叶斯收缩

对方向对齐后的因子计算逐日 Spearman IC。设第 $i$ 个因子的均值和 Newey–West 均值标准误分别为 $\mu_i$ 与 $se_i$：

$$
\tau^2=\max\left(10^{-8},\frac{1}{N}\sum_i(\mu_i^2-se_i^2)\right),
\qquad
\kappa_i=\frac{\tau^2}{\tau^2+se_i^2},
\qquad
\widetilde\mu_i=\max(\mu_i,0)\kappa_i.
$$

$\kappa_i$ 越小，样本不确定性对 IC 的收缩越强。

### 5.2 历史基线：因子级合成权重

因子相关矩阵由逐日 Spearman 相关的时间均值得到，经特征值截断修正为半正定矩阵，再逐元素平方形成冗余矩阵 $R$。信号变化率 $c_i$ 为相邻训练截面百分位排名变化绝对值的中位数。

$$
\max_w\quad
\widetilde\mu^{\mathsf T}w
-\lambda_{corr}w^{\mathsf T}Rw
-\lambda_{churn}c^{\mathsf T}w
-\lambda_{stable}\left(\lVert w-w_{prev}\rVert_2^2+w_{dropped}^2\right)
$$

约束为 $w\ge0$、$\mathbf 1^{\mathsf T}w=1$、$w_i\le0.35$，且至少 3 个因子具有有效权重。该因子级模型用于复现 v1 和进行对照，不再作为 v2 默认候选，因为它没有约束同一经济因子族的合计权重。

### 5.3 核心—残差双袖套

A3 系列把冻结的 A0 因子族得分记为核心袖套 $C_t$。增量候选先在每个决策截面对 $C_t$ 回归，最终组合只使用方向对齐后的截面残差，避免“按残差信息准入、按原始因子交易”的口径断点：

$$
f_{j,t}=a_t+b_tC_t+\varepsilon_{j,t},\qquad
S_t=(1-\lambda_t)C_t+\lambda_tR_t.
$$

A3.1 对通过增量筛选的残差候选等权，并根据费后头尾收益的一维目标估计 $\lambda_t$。2020 回归显示它提高了费前头尾收益差，但将平均单期换手由 2.69% 提高到 4.42%，费后年化主动收益由 1.70% 降到 0.99%，因此不晋级。

A3.2 保留核心、风险与股票优化接口，只改变候选袖套。候选 $j$ 的费后头尾收益均值、Newey–West 统计量、最近区块正收益比例和相邻截面百分位排名变化率分别记为 $\bar r_j$、$T_j$、$p_j$ 和 $c_j$。通过最低证据门槛后，候选权重为：

$$
q_j=\min(T_j/T_{full},1),\qquad
w_j\propto \bar r_jq_jp_j/\max(c_j,c_{floor}).
$$

候选整体的时序折外增量另设硬门槛 $T^{OOF}_{min}$ 和满置信阈值 $T^{OOF}_{full}$。候选集合是外层筛选后冻结的条件集合，但每个折外区块的内部权重只用此前区块重估。硬门槛未通过时 $\lambda_t=0$；通过后按 $\min(T^{OOF}/T^{OOF}_{full},1)$ 收缩未约束解。最后在确定性网格上选择满足下式的最大候选占比：

$$
c\!\left((1-\lambda)C+\lambda R\right)
\le \kappa c(C).
$$

冻结配置取 $T^{OOF}_{min}=1$、$T^{OOF}_{full}=2$、$\kappa=1.5$。这里的 $c(\cdot)$ 是进入收益校准和股票优化前的信号换手代理；实际目标持仓换手、成交和成本仍由完整回测独立验收。每次拟合保存逐因子证据、候选内部权重、未约束占比、预算后占比和预算是否生效。

2020—2025 年完整年度回归中，候选袖套的实际占比始终为 0。2020、2021、2024 和 2025 年的最终折外统计量均为负；2022 年虽为正，但没有通过连续区块一致性门槛；2023 年折内筛选未选出有效因子。六个年度的目标权重、成交、成本和逐日收益均与冻结核心完全一致。该分支证明了弱证据下的保护性回退，但没有产生新增 alpha。

### 5.4 预期收益

合成得分经滚动 Ridge 回归映射为 5 日预期主动收益。训练标签缩尾至 $[-20\%,20\%]$，输入得分缩尾至 $[-3,3]$；斜率和单票预期收益分别限制在 $\pm5\%$ 与 $\pm10\%$。每次拟合记录训练区间、成熟标签上界、回归系数和样本数。

## 6. 风险与主动组合优化

### 6.1 协方差与 Beta

A0 使用决策日及以前 252 个交易日的复权收益和 Ledoit–Wolf 收缩协方差。少于 120 个有效观测的股票使用 80% 年化波动率回退，目标权重不得超过调仓前权重与基准权重的较大者，因此可以复制基准但不能建立主动超配。

A1 增加一个可切换的行业与风格因子风险模型。设决策日 $t$ 的暴露矩阵为 $X_t$，列包含市场 Beta、申万行业、规模、动量、低特质波动、换手和价值；过去股票收益矩阵为 $R$。使用带 Ridge 的横截面投影得到历史因子收益和残差：

$$
F_R=RX_t(X_t^{\mathsf T}X_t+\lambda I)^{-1},
\qquad E=R-F_RX_t^{\mathsf T}.
$$

对 $F_R$ 和 $E$ 分别使用 63 日半衰期的指数权重。因子协方差向自身对角矩阵收缩 25%，各股票特异方差向截面中位数收缩 50%：

$$
\widetilde\Omega=(1-\rho_F)\Omega+\rho_F\operatorname{diag}(\Omega),
$$

$$
\widetilde d_i=(1-\rho_D)d_i+\rho_D\operatorname{median}(d),
\qquad
\Sigma_t=X_t\widetilde\Omega X_t^{\mathsf T}+\operatorname{diag}(\widetilde d).
$$

最终协方差执行对称化、特征值下限修复和缺失股票高波动率回退。模型要求至少 100 只有效股票，或至少比因子数多 2 只股票；否则显式回退到 Ledoit–Wolf，并把回退原因写入优化制品。A1 的研究门禁要求因子模型回退次数为 0。

可选的稳健 Beta 使用股票与中证 500 全收益指数不超过 252 日的收益。指数权重半衰期为 63 日，至少要求 60 个有效收益和 61 个真实价格观测。设 $\omega_s$ 为归一化时间权重：

$$
\widehat\beta_i=
\frac{\operatorname{Cov}_{\omega}(r_i,r_B)}
{\operatorname{Var}_{\omega}(r_B)},
\qquad
\widetilde\beta_i=
\operatorname{clip}\left(0.75\widehat\beta_i+0.25,\ 0.25,\ 1.75\right).
$$

缺少足够真实价格的股票使用 Beta 1，而不是把前向填充生成的零收益误计为完整历史。优化器、目标持仓审计和成交后审计使用同一组 Beta；原 60 日特征 Beta 仍单独保存，便于对账。

### 6.2 主动组合优化

设 $w$ 为目标权重，$b$ 为当期基准权重，$w_0$ 为调仓前权重，$\widehat\mu$ 为预期收益，$\Sigma$ 为日频协方差，$h=5$ 为风险期限：

$$
\max_w\quad
\widehat\mu^{\mathsf T}(w-b)
-\gamma(w-b)^{\mathsf T}(h\Sigma)(w-b)
-c_b\lVert(w-w_0)^+\rVert_1
-c_s\lVert(w_0-w)^+\rVert_1
-\eta\lVert w-b\rVert_2^2.
$$

基准参数为 $\gamma=3$、$\eta=0.01$。买入成本包含 5 bps 线性费用；卖出成本另含适用日期的印花税。满仓、多头、单边换手、不可买卖方向和事前 ADV 交易上限作为硬约束。其余配置上限为：

- 多头满仓；
- 单票权重不高于 3%；
- 单票主动权重位于 $[-1\%,1\%]$；
- 单边换手不高于 35%，首次建仓上限为 100%；
- 行业主动暴露不高于 2%；
- 年化事前跟踪误差不高于 5%；
- 单次交易不超过决策日收盘可得的 20 日 ADV 的 5%；
- 不可买、不可卖和数据不合格股票遵守方向约束。

为避免硬交易条件使整个问题无解，个股权重、主动权重、行业、贝塔和跟踪误差上限各自带非负缺口。设 $G(w)$ 为这些缺口除以对应上限后的总和，其中跟踪误差使用年化方差缺口除以 $5\%^2$。实现先求解一次 $G(w)=0$ 的原目标；若可行，则已知字典序最优缺口为零并直接返回。只有严格上限不可行时才进入以下两阶段：

$$
G^*=\min_w G(w),
$$

$$
\max_w\;\text{原收益—风险—成本目标}
\quad\text{s.t.}\quad G(w)\le G^*+\varepsilon.
$$

因此，收益目标不能用增加配置缺口来换取更高预测收益；只有不可交易、换手或容量等硬条件造成客观不可达时才保留最小缺口。若第二阶段失败但第一阶段解有效，系统返回 `repair_only` 并交易至最小缺口组合；该状态不计入正式求解率。求解后以原始数值重新计算满仓、多头、换手、交易方向和单票交易上限。最小缺口锁使用无量纲的标准化分数，校验时先乘以所有修复项分母中的最大值，换算为保守的约束残差，再与 $10^{-6}$ 数值容差比较，避免把无量纲分数直接与权重残差混合。

成交审计不再把调仓前持仓作为永久约束边界。它分别记录目标组合的配置缺口、实际组合的配置缺口和成交相对目标造成的增量恶化，并报告目标与实际的跟踪误差及主动风险利用率。数值容差为 $10^{-6}$，物质性阈值为 1 bp；发布门槛使用物质性违规率，避免把求解器浮点残差误记为经济性违规。

## 7. 执行与收益核算

执行器在下一开放日按复权开盘价先卖后买。买单在现金不足时等比例缩放；订单受停牌、涨跌停、ST 禁买和 ADV 容量限制，并记录完全成交、部分成交或阻断原因。

流动性输入按决策日收盘冻结：以截至该日的历史成交额计算 20 日 ADV，同一快照同时用于优化器交易上限、开盘成交容量和冲击成本。快照日期必须等于信号日并严格早于执行日；缺少有效事前 ADV 时订单被阻断。执行日全天成交额在成交决定结束后才写入审计，只计算事后全天参与率，不改变开盘成交结果。

成本模型包括：

- 买卖双边 5 bps 线性费用；
- 卖出印花税：2023-08-28 前为 10 bps，之后为 5 bps；
- 随成交金额和 ADV 参与率变化的冲击成本，在 5% ADV 处为 10 bps。

回测采用连续份额记账。组合净值、基准净值、主动收益、跟踪误差、信息比率、回撤、换手、成交金额完成率和实际成本均由逐日或逐笔记录计算。

### 7.1 年度滚动编排

年度编排器从一个有界方法 Study 生成候选方法与 2020—2025 年的笛卡尔任务矩阵。年度边界由冻结交易日历和调仓序列确定；每折保存训练截止日、最后允许标签可得日、隔离起点、首末评价调仓日和数据快照哈希。原始因子与截面处理结果只构造一次，每折标签视图在该折结束日截断，同年度候选只读复用。

任务可以按折并行，单个任务的优化器保持原有确定性设置。每个任务使用独立尝试目录；恢复时先核验任务身份和全部制品哈希，仅失败任务重跑。六折合并前检查日期唯一、折间无覆盖、成本合同一致、方法参数跨折不漂移、评价期模型只在首个调仓日拟合，以及模型和收益校准使用的最大标签可得日不晚于隔离带前一开放日。各折重置净值不会直接拼接，合并净值从逐日组合与基准收益重新计算。

2020 年 v2 单折正式验收与恢复测试已经通过：48 次优化全部求解，关键制品哈希一致，重复运行跳过原任务且不增加尝试次数。该折的费后主动收益为 1.36%，信息比率为 0.27，尚未达到发布门槛。验收发现原优化器允许调仓前的主动权重、行业和贝塔违约原幅保留，导致求解状态正常但配置上限没有被逐步修复。

该问题已在 v3 代码与协议中按上述两阶段方法修复。v3 使用独立年度研究标识，新增实际组合物质性配置违规率、成交增量恶化率和主动风险利用率门槛；旧 v2 制品只保留为已揭示诊断。

v3 的 2020 年正式单折已在干净提交 `5ef8a76` 上完成。共享特征首次物化耗时约 21 分 42 秒，折任务耗时 8 分 39 秒，整次运行约 30 分 46 秒。目标组合配置违规率降至 0，成交后物质性配置违规率为 2.56%；但冻结制品中 48 次优化只有 39 次被接受，记录求解率为 81.25%，年化主动收益为 1.70%，信息比率为 0.34。

逐次对账表明，9 次拒绝均为 `postsolve_infeasible`：8 次严格问题已返回最优，另 1 次修复和二阶段收益问题均返回最优。唯一超过阈值的字段是无量纲最小缺口锁分数，超额为 $1.07\times10^{-6}$ 至 $5.20\times10^{-6}$；其余硬约束残差最大仅 $1.53\times10^{-8}$。按启用贝塔约束后的最保守尺度 0.05 换算，缺口锁残差最大为 $2.60\times10^{-7}$，低于 $10^{-6}$ 容差。旧实现把无量纲分数与权重残差直接取最大值，造成数值有效解的误拒绝。

v3 制品保持冻结，不追溯改写收益。单位一致的 v4 已使用独立研究标识完成相同 2020 年折：48 次优化全部被接受，成交后物质性配置违规率为 2.08%，年化主动收益为 1.70%，信息比率为 0.34。该折的平均日度 Rank IC 为正，但 CAPM Beta 为 0.904，60 日滚动 Beta 偏离绝对值的 95% 分位数为 0.181。该诊断促成了 A1 的 2×2 风险翻译实验；v4 结果不进入 A1 选模。

## 8. 实证结果

### 8.1 方法选择期消融

固定筛选器、风险、优化、执行和收益校准，仅逐步增加 IC 收缩、相关性、信号变化率和跨期权重稳定项：

| 方法 | 信息比率 | 平均权重 L1 变化 |
|---|---:|---:|
| C0 原始受约束 IC | 0.719 | 0.197 |
| C1 不确定性收缩 | 0.719 | 0.203 |
| C2 相关性惩罚 | 0.719 | 0.219 |
| C3 换手惩罚 | 0.719 | 0.215 |
| C4 权重稳定项 | 0.675 | 0.156 |

C4 降低了跨期权重变化，但同一评价窗口的信息比率下降。该消融不支持“稳定项单独改善净收益”的结论。

### 8.2 扩展验证

| 指标 | 方向等权 | IC 合成 |
|---|---:|---:|
| 总收益 | 45.64% | 46.50% |
| 年化主动收益 | 5.13% | 5.20% |
| 信息比率 | 1.141 | 1.271 |
| 最大回撤 | -28.57% | -27.79% |
| 平均换手 | 3.22% | 1.24% |
| 执行成本 | 10.58 bps | 9.89 bps |
| 最差年度信息比率 | 0.823 | 0.016 |

IC 合成在 2023—2025 年总体信息比率较高且换手较低，但 2025 年信息比率仅为 0.016，年度稳定性弱于方向等权。

### 8.3 v1 已揭盲评价窗口

v1 模型在 2026-01-09 拟合，训练标签最晚可得日为 2025-12-18。10 个入选因子中，实质权重集中于 `low_idio_vol_60` 35%、`low_range_vol_20` 35% 和 `free_turnover_20` 30%。该窗口现已用于模型诊断，不能再次作为独立样本外证据。

| 指标 | 结果 |
|---|---:|
| 组合 / 基准总收益 | 4.39% / 12.10% |
| 年化主动收益 | -16.98% |
| 跟踪误差 / 信息比率 | 7.43% / -2.286 |
| 最大回撤 | -12.27% |
| 平均换手 | 1.92% |
| 执行成本 | 7.78 bps |
| 成交金额完成率 | 99.92% |

最终窗口的日均 Rank IC 为 0.0246，五分组单调性为 -0.90，事后收益校准斜率为 -1.802。最高预期收益组的实际主动收益低于最低组，排序信号没有转化为正主动收益。

### 8.4 A1 风险翻译

A1 固定全部信号与执行规则，只比较当前风险模型、EWMA 收缩 Beta、行业与风格因子协方差及两者组合。24 个年度折均已完成。旧年度聚合器最初拒绝了 2023 年制品，因为该年每次重训都合法返回 `no_selected_factors`，却被按“存在多个验证期拟合”处理；聚合审计现允许从首个决策日起持续关闭且从未晚期拟合的空模型序列，同时仍拒绝晚期才成功的模型。

六折逐日序列复算结果如下：

| 方法 | 年化主动收益 | 信息比率 | 正收益年度 |
|---|---:|---:|---:|
| 当前风险模型 | 1.64% | 0.388 | 5/6 |
| EWMA 收缩 Beta | 1.47% | 0.341 | 5/6 |
| 因子协方差 | 1.77% | 0.413 | 5/6 |
| 因子协方差 + EWMA Beta | 1.66% | 0.378 | 5/6 |

因子协方差的增益不足预登记晋级线，并在 2024—2025 年弱于当前模型，因此 A1 不替换 A0。

### 8.5 A2.1 无模型因子审计

A2.1 先在 2017—2019 年发现期审计 7 个候选，再用固定 v2 方向审计 2020—2025 年开发验证期：

| 区间 | 调仓截面 | 股票记录 | 通过候选 | 时点违规 |
|---|---:|---:|---:|---:|
| 2017—2019 | 147 | 73,500 | 6/7 | 0 |
| 2020—2025 | 291 | 145,500 | 3/7 | 0 |

验证期继续通过的是 `turnover_volatility_20`、`high_turnover_return_20` 和 `limit_up_close_rate_20`。其年度有向 IC 中位数分别为 0.0389、0.0303 和 0.0102；五日费后 Q5−Q1 年度中位数分别为 0.0955%、0.0070% 和 0.0491%。三者仍存在费后负价差年度，必须进入折内筛选和组合回测，不能把无模型审计收益当作可实现策略收益。

年度 A2 实验没有按验证期结果把候选缩到三项，而是把发现期通过的六项全部交给各年度折内筛选器。对照组与扩展组物化同一 48 因子特征层，对照组通过可审计排除名单恢复冻结的 42 因子池，从而只识别候选池扩展的作用。

### 8.6 A3.1 残差袖套诊断

A3.1 的 2020 年固定折中，A0 信号、逐日结果和指标均精确复现，48 次股票优化全部返回有效解。残差候选的 Rank IC 为 0.0659，费前头尾收益差为 0.418%，均不弱于 A0；但候选占比在最后一次拟合达到 30.7%，候选合成分数的排名变化率约为核心的 6.5 倍。

| 指标 | A0 | A3.1 |
|---|---:|---:|
| 年化主动收益 | 1.70% | 0.99% |
| 信息比率 | 0.344 | 0.224 |
| 主动最大回撤 | -6.05% | -4.47% |
| 平均单期换手 | 2.69% | 4.42% |
| 累计交易成本 | 0.88% | 1.45% |

目标组合费前主动收益相对 A0 只下降约 8 bp，而额外成本约为 57 bp，说明主要断点位于候选权重和交易翻译。该折已用于 A3.2 设计，只保留为开发回归，不再构成独立验证；A3.1 未开放 2021 年。

### 8.7 A3.2 与 v2 冻结

最终年度复算在同一数据快照上完成 2020—2025 年两种方法的 12 个任务，输入快照的 412 个哈希均匹配，运行期间没有网络请求。A3.2 的候选袖套证据如下：

| 年度 | 最终折外增量 $t$ | 门禁结果 | 最大实际候选占比 |
|---:|---:|---|---:|
| 2020 | -0.976 | 未通过 | 0% |
| 2021 | -1.924 | 未通过 | 0% |
| 2022 | 1.434 | 连续区块一致性未通过 | 0% |
| 2023 | — | 折内无入选因子 | 0% |
| 2024 | -1.165 | 未通过 | 0% |
| 2025 | -0.526 | 未通过 | 0% |

由于所有年度的候选占比均为 0，A3.2 与 A0 的目标权重、成交、交易成本和逐日收益逐项一致。最终冻结方案的 2020—2025 年年化主动收益为 1.64%，信息比率为 0.388，累计相对收益为 9.76%，主动最大回撤为 -6.64%，5/6 个年度主动收益为正。

预先登记的 12 项发布门槛通过 8 项；年化主动收益、信息比率、主动最大回撤和滚动 Beta 偏离未通过。A3.2 的保护性回退达到工程目标，但没有达到新增 alpha 的研究目标，因此 v2 发布冻结 A0 的滚动权重生成机制。A3、A3.1 与 A3.2 保留为可复算的未晋级证据，不把保护性回退表述为收益改进。

## 9. 审计与测试

每次研究运行记录配置、研究规格、源码、依赖版本、数据快照、组件参数、拟合样本范围、输出制品和 Git 状态。v1 最终留出报告重新核验运行制品、研究输入和协议锁，并从逐日净值与成交记录独立复算指标；v2 年度研究另外核验年度任务状态、两种方法的可比性、聚合制品哈希和公开报告来源。

该次冻结运行的审计结果：

- 18 个运行制品、5 个运行级研究表和 13 张标准化输入表的哈希差异均为 0；
- 日期越界、标签时点违规和自然键重复均为 0；
- 23 次优化全部求解，最大约束违规为 $2.45\times10^{-8}$；
- 独立指标复算的最大绝对差为 0；
- 历史行业覆盖率存在一个非阻断告警，最低值为 83.2%。

合成测试覆盖公告生效日成分切换、调入股冻结代理权重、未来数据注入、标签成熟度、隔离期、阶段冻结、年度折生成、共享特征等价性、并行任务恢复、制品哈希、跨折合并、名称与 ST 区间、行业边界、风险窗口、优化约束、不可交易方向、事前 ADV 容量、执行日成交量不变性、部分成交、成本、数据锁和报告完整性。

因子池另有独立的无模型审计入口。它可显式选择 A0 或 A2 提供器，在每个调仓截面统一计算原始覆盖率、异常值分布、行业依赖、有向 Rank IC、方向一致的五分组收益、换手、模型化成本和成交容量，并按年度汇总 Newey–West 统计量与正向年份比例。基本面因子同时检查公告日越界、陈旧值和未来报告期；任一违规都会使因子失去资格。全时期审计只用于检查候选池和研究实现，正式模型仍须在每个滚动训练折内重新筛选，不能把全时期通过名单直接回填到历史组合。

审计产物包括因子目录、原始与处理后特征、标签、调仓截面价差、逐年结果、因子汇总、相关矩阵、行业分布和运行清单。运行清单记录数据与代码指纹，且审计命令不发起网络请求。

基于财务数据合同 v2 的正式审计覆盖 2017—2025 年 438 个调仓截面、21.9 万条股票记录和 42 个候选因子。14 个因子通过预先冻结的覆盖率、样本长度、正向年份比例、逐年有向 IC 中位数和逐年费后 Q5−Q1 中位数门槛；公告日越界、陈旧值参与计算和未来报告期违规均为 0。该结果确认候选池中存在可继续研究的信号，但不构成最终模型入选名单。

通过者中存在显著冗余：三个低风险代理的两两相关系数为 0.67—0.81，并且 2025 年费后价差均为负；现金流收益率、现金资产收益率和应计因子也共享较多信息。后续筛选必须在各训练折内按相关簇限额，不能把同一经济来源重复计入有效因子数。

发布前的数据就绪度审计进一步核验了 269.1 万条日线、每日估值与换手、复权、涨跌停及四类财务表：日线自然键重复、必需字段缺失、非正价格和 OHLC 价序违规均为 0；每日估值与换手相对日线覆盖率为 99.9999%；财务表的公告日和交易可得日无缺失，也没有可得日早于源公告日的记录。42 个候选的平均截面覆盖率为 95.1%，全部时点违规为 0。因此当前模型上限不能归因于底层数据质量失败。

限制来自信息宽度和稳定性。14 个门禁通过者在绝对相关系数 0.75 下形成 12 个相关簇；2025 年只有 7/14 的费后头尾收益差为正，中位数为每调仓期 0.71 bp。资金流、两融和公告数据尚未完成本地缓存、覆盖验证、时点合同、统一特征接口与正式审计，即使 API 权限可用也不进入 v2。完整复算见 `notebooks/v2_factor_data_readiness.ipynb`。

折内筛选与族级合成已完成真实快照核验。使用正式审计快照、最近 156 个训练截面和冻结门槛，2024-01-02 与 2025-01-02 分别有 6 个和 10 个因子通过；低特质波动、低下行波动、低振幅波动、短期波动和最大单日收益被识别为同一传递相关簇，最终只保留一个代表。2024-01-02 的 6 个因子分属风险、流动性、规模和价值四族，方向等权、正则 Ridge 与稳健 IC 三种族级模型均成功拟合；单因子最高权重为 20%，单族最高权重为 35%，有效因子数约为 5.8。该检查只验证训练折接口与约束，不作为收益结论。

## 10. 仓库与复现

```text
configs/                 研究、实验矩阵与压力情景配置
notebooks/               聚合证据复算与研究决策审计
scripts/                 数据准备入口
src/csi500_alpha/data/   请求、缓存、标准化与质量检查
src/csi500_alpha/features/ 因子、截面处理与标签
src/csi500_alpha/workflow/ 筛选、预测与收益校准
src/csi500_alpha/risk/   协方差估计
src/csi500_alpha/portfolio/ 主动组合优化
src/csi500_alpha/execution/ 事件执行与回测
src/csi500_alpha/annual.py  年度滚动任务编排与恢复
tests/                   不依赖 Token 的合成测试
docs/assets/             公开聚合结果、图表与来源哈希
docs/archive/            历史展示文档，仅用于版本追溯
```

```powershell
uv sync --extra dev --extra report
Copy-Item .env.example .env

uv run python -m csi500_alpha doctor --config configs/full.yaml
.\scripts\download_tushare.ps1
.\scripts\download_eligibility.ps1
.\scripts\download_financial.ps1 -PlanOnly
.\scripts\download_financial.ps1
.\scripts\run_factor_audit.ps1 -PlanOnly
.\scripts\run_factor_audit.ps1
.\scripts\run_factor_audit.ps1 -Spec .\configs\factor_audit_a2_discovery.yaml
.\scripts\run_factor_audit.ps1 -Spec .\configs\factor_audit_a2_price_volume.yaml

uv run python -m csi500_alpha plan-study --study configs/studies/factor_family_v2.yaml
.\scripts\run_annual_study.ps1 `
  -Mode Plan `
  -AnnualConfig .\configs\annual\a2_factor_expansion_v1.yaml
.\scripts\run_annual_study.ps1 `
  -Mode Run `
  -AnnualConfig .\configs\annual\a2_factor_expansion_v1.yaml `
  -Year 2020 `
  -Workers 2
.\scripts\run_v2_release.ps1 -Mode Plan
.\scripts\run_v2_release.ps1 -Mode Run -Workers 4

uv run python -m csi500_alpha build-v2-readme-report `
  --baseline-root .\annual-studies\a2-factor-expansion-v1-annual-2020-2025\aggregates\a0_frozen_pool `
  --expanded-root .\annual-studies\a2-factor-expansion-v1-annual-2020-2025\aggregates\a2_discovery_pool `
  --selection-root .\annual-studies\a32-turnover-budgeted-residual-v1-canonical-annual-2020-2025 `
  --factor-audit-root .\runs\factor-audits\<正式审计运行目录> `
  --output .\docs\assets

uv run python -m pytest
uv run ruff check .
uv run mypy src
uv lock --check
```

长任务按阶段输出进度；截面构建、财务因子、截面处理、滚动信号、收益校准和事件回测每隔约 30 秒报告完成量、总量、当前日期、累计耗时与预计剩余时间。年度入口同时在年度目录和当前折的 `attempt-*` 目录写入 `progress.json`，供另一个终端或监控程序读取；最终研究结论仍以带哈希的正式清单和制品为准。

## 11. 许可与实现边界

原创代码和文档采用 MIT 许可证。市场数据来自 Tushare Pro，使用者须自行提供 Token 并遵守供应商条款；MIT 许可证不授予市场数据再分发权。

当前结果是基于模型化成本和容量的历史模拟，不代表实盘成交。2026 年上半年已经揭盲，只保留为 v1 失效样本。执行器采用连续份额，未包含 100 股整数手和完整公司行动台账。本文及仓库内容不构成投资建议。
